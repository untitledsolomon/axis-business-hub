# Generate invoice PDF

This function renders the organisation's built-in or custom HTML invoice template through PDFShift.

Set the API key as an edge-function secret before deploying:

```sh
supabase secrets set PDFSHIFT_API_KEY=your_api_key
```
